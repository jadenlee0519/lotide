
const assertEqual = require('../assertEqual')


describe('chenck two arry', () => {
  it('chech two array', () => {
    assertEqual("Lighthouse Labs", "Bootcamp");
  });

  it('chech two array', () => {
    assertEqual(1, 1);
  });
});


